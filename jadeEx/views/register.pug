doctype html 
html 
  head 
    title Đăng Nhập 
    link(rel="stylesheet" href="/css/styles.css") 
  body 
    .login-container 
      h1 Đăng Nhập 
      form(action="/register" method="POST") 
        .form-group 
          label(for="username") Tên tài khoản 
          input#username(type="text" name="username" placeholder="Nhập tên tài khoản" 
            required) 
        .form-group 
          label(for="password") Mật khẩu 
          input#password(type="password" name="password" placeholder="Nhập mật khẩu" 
            required) 
        .form-group 
          label(for="confirmpassword") Nhập lại Mật khẩu 
          input#confirmpassword(type="confirmpassword" name="confirmpassword" placeholder="Nhập lại mật khẩu" 
            required) 
        .form-group 
          label(for="email") Emai  
          input#email(type="email" name="email" placeholder="Nhập email" 
            required) 
        .form-group 
          button(type="submit") Đăng Nhập
             .form-group 
          a(href="/register") Đăng ký tài khoản mới